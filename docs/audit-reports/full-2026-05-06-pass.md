# 全体監査レポート（再監査・モジュール 0–11 全体）

- 監査日: 2026-05-06
- 対象: 1 回目監査（`docs/audit-reports/full-2026-05-06.md`）の指摘事項を反映後の `huriko-mausu`（コミット a6844b7）
- 基準: `docs/spec.md`（正典・改訂反映済）、`docs/design.md` + `docs/design-handoff/`（UI 参照）、`docs/conventions.md`（規約）

## 総合判定
**承認**

1 回目の F-1〜F-5、Q-1、Q-3、O-1 は実装修正および spec 改訂で全て整合済み。新規の重大不適合は検出されず。

---

## 1 回目指摘事項の対応確認

| ID | 内容 | 反映状態 | 確認箇所 |
|----|------|---------|----------|
| F-1 | `HS_STORAGE_KEY` を `'furiko-aim-highscores-v2'` に修正 | ✓ | `src/constants.ts:53` |
| F-2 | `vite.config.ts` に `base: '/furiko-aim/'` を追加 | ✓ | `vite.config.ts:8` / `dist/index.html` のアセット参照 |
| F-3 | PAUSE ピル表記。spec を ESC のみに改訂、SPACE は内部処理として有効 | ✓ | `src/components/HUD.tsx:58` / `docs/spec.md` 3.4 |
| F-4 | 結果画面右ブロック数値。spec をハンドオフ準拠 32px・サイドバー的に控えめへ改訂 | ✓ | `src/styles/game.css:894` / `docs/spec.md` 3.5 |
| F-5 | `index.html` を `lang="ja"` / `<title>振り子エイム</title>` に修正 | ✓ | `index.html:2,7` / `dist/index.html:2,7` |
| Q-1 | カスタム結果画面でも難易度・ハイスコア・プレイ時間メタを表示 | ✓ | `src/screens/ResultScreen.tsx:88-109` |
| Q-2 | カスタムリトライはユーザー指定値そのまま（spec 2.5.2 尊重で現状維持） | 維持 | ユーザー方針通り |
| Q-3 | `strict: true` を tsconfig に追加 | ✓ | `tsconfig.app.json:19` / `tsconfig.node.json:18` |
| O-1 | `src/App.css` 削除 | ✓ | `src/` 配下に存在せず |
| O-2 | README は範囲外として未対応 | 範囲外 | — |
| O-3 | `useGameLoop` の `onFinish` 参照不安定。性能上の僅少。範囲外 | 範囲外 | — |
| O-4 | `CustomSlider` の useEffect 依存配列なし。性能上の僅少。範囲外 | 範囲外 | — |

---

## 動的確認

### ビルド
- コマンド: `npm run build`（= `tsc -b && vite build`）
- 結果: 成功（44 modules / `dist/index.html` 0.50 kB / JS 224.63 kB / CSS 18.30 kB）
- `dist/index.html` に `/furiko-aim/` 起点のアセットパスが正しく出力されていることを確認
- `lang="ja"` / `<title>振り子エイム</title>` も確認

### 起動
- 未実施（ビルド成功・コードレビュー・dist 出力のパス確認でカバー済みと判断）

### 外部 I/O
- 外部 API 呼び出しなし。localStorage / Web Share API / Twitter intent はブラウザ標準で動的呼び出し対象なし

---

## モジュール別カバレッジ（spec 7 / dev-guide）
- M0 規約: `docs/conventions.md` あり、命名・構造・strict 設定すべて準拠
- M1 物理: `src/lib/physics.ts` で n 振り子 RK4・サブステップ実装。OK
- M2 難易度・カオス保証: `src/constants.ts:PROFILES` 6 プロファイル、`src/lib/chaos.ts` 実装。OK
- M3 描画: `PendulumCanvas.tsx` で軌跡 500ms フェード・フィールド円・accent ring。OK
- M4 入力・スコア: `src/lib/scoring.ts` で `tip + trail` の max（spec 2.2）、`σ = totalLenPx × 0.06`、`σ_t = σ × 1.5`、`3σ` 裾切り。OK
- M5 ゲームループ: `useGameLoop.ts` で 60 秒タイマー・3 秒カウントダウン・ESC/SPACE 一時停止・累積。OK
- M6 UI 画面遷移: タイトル → モード → 難易度 / カスタム → ゲーム → 結果。OK
- M7 ハイスコア: `lib/storage.ts` + `useHighScores.ts`、ストレージキー `furiko-aim-highscores-v2`。OK
- M8 結果画面 PNG / 共有: `lib/canvasShot.ts`、`lib/share.ts`。OK
- M9 カスタムドラッグ: `PendulumCanvas.tsx editable`、`AngleDial`、`CustomSlider`。OK
- M10 レスポンシブ・GH Pages 準備: CSS の `@media` ブレイクポイントあり。`vite.config.ts` の base 設定済。OK
- M11 ハンドオフ準拠の最終整合：HUD・PAUSE オーバーレイ・カウントダウン・カスタムスライダー・ダイヤル・難易度カードすべてハンドオフ準拠。OK

---

## 仕様適合（追加確認）
- spec 2.2 距離スコア式・係数（`SIGMA_FACTOR_TIP=0.06` / `SIGMA_FACTOR_TRAIL_MULT=1.5` / `TRAIL_SCORE_WEIGHT=0.55` / `SCORE_CUTOFF_SIGMA=3`）すべて一致
- spec 2.3 物理：`GRAVITY=9.81` / `RK4_SUBSTEPS=8` / 減衰なし。一致
- spec 2.4 フィールド：`FIELD_RADIUS_RATIO=0.42`。一致
- spec 2.8 軌跡：`TRAIL_LIFETIME_MS=500`、glow 固定。一致
- spec 2.10 ハイスコア：localStorage キー / KNOWN_KEYS（6 ノーマル + 2 カスタム）の整合。OK
- spec 2.11 / 2.12 カウントダウン・一時停止：3 秒、ESC/SPACE トグル、AIM HERE マーカー、PAUSE オーバーレイ 4 ボタン。OK
- spec 2.13 Tweaks：UI 非マウント。`FIXED_TWEAKS` は定義のみで未使用（spec 「実装ファイルとしては残してもよい」を充足）
- spec 2.14 アクセント色：`ACCENT_COLOR='#d21e1e'`。一致
- spec 5 入力：マウスのみ、タッチ非対応。一致

---

## 観察事項（範囲外・要対応なし）
- `FIXED_TWEAKS` は定数定義のみで参照箇所なし（spec 2.13 が許容）
- `useGameLoop` の `onFinish` 依存、`CustomSlider` の useEffect 依存配列：1 回目 O-3 / O-4 で範囲外指定。機能上は無害

---

## Git
- ワーキングツリー clean。追加コミットなし
- 直近コミット: a6844b7「全体監査（1回目）修正反映 + レポートアーカイブ」

---

## 結論
全モジュールが spec.md および conventions.md に整合。ビルドも成功。デプロイ準備モジュール（M10 範囲）まで完了状態として承認。
