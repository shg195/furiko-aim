# dev-guide 動作設定

## 部分監査の動作モード
- **毎回確認してから実行**
- モジュール完了ゲート時に AskUserQuestion で「実行 / スキップ（理由要） / 修正してから」を提示
- 「実行」選択時に Skill ツールで `dev-audit-1-partial` を呼び出す

## 公開・配布の予定
- **ある：GitHub Pages で Web 公開**
- 対象種別：Web（SPA、静的ホスティング）
- spec.md「6. 技術スタック」「8. 実装範囲外」と整合

## Tweaks パネル方針
- **本番では非マウント**（UI を隠す。既定値固定）
- spec.md 2.13 / 2.14 に明記
